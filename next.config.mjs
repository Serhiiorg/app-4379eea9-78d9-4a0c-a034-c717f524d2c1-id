/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['fonts.googleapis.com', 'images.unsplash.com', 'instagram.com"', 'facebook.com"', 'twitter.com"', 'images.unsplash.com', 'www.w3.org'],
      },
    };
    
    export default nextConfig;